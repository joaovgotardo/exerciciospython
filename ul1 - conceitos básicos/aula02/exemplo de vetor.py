v_filiais = ['filial concordia', 'filial ipumirim', 'filial lindoia do sul'] #nomenclatura de listas é feita por colchetes
#print(v_filiais)
print(len(v_filiais)) #len - abreviação de length, ou tamanho, diz o comprimento de uma lista

for item in range(len(v_filiais)): #será imprimido como uma coluna #range é um contador em python
    #range é um espaço entre uma certa quantidade
    print(v_filiais[item])

#como incluir um novo dado no vetor?

